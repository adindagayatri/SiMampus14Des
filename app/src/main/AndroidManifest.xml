<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.simampuscrud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Member.Member"
            android:label="Member"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".Member.EditMember" />
        <activity android:name=".Member.AddMember" />
        <activity android:name=".Peminjaman.AddEditPinjam" />
        <activity android:name=".Buku.EditBuku" />
        <activity
            android:name=".Peminjaman.Pinjam"
            android:label="@string/title_activity_pinjam"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Buku.AddBuku" />
        <activity
            android:name=".Buku.Buku"
            android:label="Buku"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginRegister.LoginAdmin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginRegister.RegisterAdmin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>